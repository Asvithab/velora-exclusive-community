kind = "mobile"
previewPath = "/"
title = "VELORA – Exclusive Members Community"
version = "1.0.0"
id = "artifacts/velora"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/" ]
localPort = 18783

[services.development]
run = "pnpm --filter @workspace/velora run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/velora", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/velora", "run", "serve" ]

[services.env]
PORT = "18783"
BASE_PATH = "/"
